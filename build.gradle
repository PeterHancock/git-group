/*
    This gradle build is solely used to create the READMEs
*/

buildscript {
    dependencies {
        classpath files('readme/readme.jar')
    }
}

apply plugin: 'readme'

task createWrapper(type:Wrapper) {
    gradleVersion = '1.0-milestone-8a'
}

defaultTasks 'createReadme'

